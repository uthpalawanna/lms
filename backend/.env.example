# Copy this file to ".env" and fill in your real values.
# Never commit the real ".env" file to GitHub.

# Get this from MongoDB Atlas after creating a free cluster (steps in README.md)
MONGO_URI=mongodb+srv://uthpalawanna30_db_user:user1234@cluster0.hpz7hma.mongodb.net/lms

# Any long random string — used to sign login tokens. Example below is fine to use.
JWT_SECRET=change_this_to_a_long_random_string_12345

# Port the backend server will run on
PORT=5000
